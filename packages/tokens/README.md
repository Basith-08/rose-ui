# @kagami.b/tokens

Publishable design tokens for Rose UI.

## Installation

```bash
pnpm add @kagami.b/tokens
```

## Usage

```ts
import { colors, spacing, radius } from "@kagami.b/tokens";
```

The package ships compiled ESM and TypeScript declarations from `dist/`.
